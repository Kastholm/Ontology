{
  "scripts": {
    "init": "npm i && pip install rdflib",
    "start": "tsc && node genOntology.js && python convert.py",
    "macstart": "tsc && node genOntology.js && python3 convert.py"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "fs": "^0.0.1-security",
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "@types/xmldom": "^0.1.34"
  }
}
